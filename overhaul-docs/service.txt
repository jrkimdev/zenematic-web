<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>About — Zenematic Studio</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,500;0,600;1,300;1,400&family=Outfit:wght@200;300;400;500&display=swap" rel="stylesheet">
<style>
  :root {
    --black: #0a0a0a;
    --studio: #141414;
    --shadow: #1e1e1e;
    --film: #f0ede6;
    --white: #ffffff;
    --gold: #c8a96e;
    --gold-dim: rgba(200,169,110,0.12);
    --gray: #888;
  }

  *, *::before, *::after { margin: 0; padding: 0; box-sizing: border-box; }
  html { scroll-behavior: smooth; }

  body {
    background: var(--black);
    color: var(--film);
    font-family: 'Outfit', sans-serif;
    font-weight: 300;
    overflow-x: hidden;
  }

  /* NAV */
  nav {
    position: fixed;
    top: 0; left: 0; right: 0;
    z-index: 100;
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 1.5rem 4rem;
    background: linear-gradient(to bottom, rgba(10,10,10,0.95), transparent);
  }

  .nav-logo {
    font-family: 'Outfit', sans-serif;
    font-weight: 200;
    font-size: 1.1rem;
    letter-spacing: 0.25em;
    text-transform: uppercase;
    color: var(--white);
    text-decoration: none;
  }

  .nav-logo span { color: var(--gold); }

  .nav-links {
    display: flex;
    gap: 2.5rem;
    list-style: none;
  }

  .nav-links a {
    font-size: 0.75rem;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: rgba(255,255,255,0.55);
    text-decoration: none;
    transition: color 0.3s;
  }

  .nav-links a:hover,
  .nav-links a.active { color: var(--white); }

  .nav-cta {
    font-size: 0.75rem;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: var(--gold);
    text-decoration: none;
    border: 0.5px solid rgba(200,169,110,0.4);
    padding: 0.6rem 1.4rem;
    transition: all 0.3s;
  }

  .nav-cta:hover {
    background: var(--gold-dim);
    border-color: var(--gold);
  }

  /* PAGE HERO */
  .page-hero {
    padding: 14rem 4rem 8rem;
    background: var(--black);
    position: relative;
    overflow: hidden;
  }

  .page-hero::before {
    content: '';
    position: absolute;
    top: 0; right: 0;
    width: 50%;
    height: 100%;
    background: radial-gradient(ellipse at top right, rgba(200,169,110,0.04) 0%, transparent 70%);
    pointer-events: none;
  }

  .page-hero-label {
    font-size: 0.65rem;
    letter-spacing: 0.25em;
    text-transform: uppercase;
    color: var(--gold);
    margin-bottom: 2rem;
    opacity: 0;
    animation: fadeUp 1s ease 0.2s forwards;
  }

  .page-hero-headline {
    font-family: 'Cormorant Garamond', serif;
    font-weight: 300;
    font-size: clamp(3.5rem, 7vw, 6.5rem);
    line-height: 1.02;
    color: var(--white);
    max-width: 820px;
    opacity: 0;
    animation: fadeUp 1s ease 0.4s forwards;
  }

  .page-hero-headline em {
    font-style: italic;
    color: var(--film);
  }

  @keyframes fadeUp {
    from { opacity: 0; transform: translateY(24px); }
    to { opacity: 1; transform: translateY(0); }
  }

  /* FOUNDER SECTION */
  .founder {
    padding: 6rem 4rem;
    background: var(--studio);
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 6rem;
    align-items: start;
  }

  .founder-image-col {
    position: relative;
  }

  .founder-image {
    width: 100%;
    aspect-ratio: 3/4;
    background: var(--shadow);
    position: relative;
    overflow: hidden;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .founder-image-placeholder {
    font-size: 0.65rem;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: rgba(255,255,255,0.1);
    text-align: center;
    line-height: 2;
  }

  /* Gold corner accent */
  .founder-image::before {
    content: '';
    position: absolute;
    top: -1px; left: -1px;
    width: 40px; height: 40px;
    border-top: 0.5px solid var(--gold);
    border-left: 0.5px solid var(--gold);
    opacity: 0.5;
    z-index: 1;
  }

  .founder-image::after {
    content: '';
    position: absolute;
    bottom: -1px; right: -1px;
    width: 40px; height: 40px;
    border-bottom: 0.5px solid var(--gold);
    border-right: 0.5px solid var(--gold);
    opacity: 0.5;
    z-index: 1;
  }

  .founder-caption {
    margin-top: 1.25rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .founder-name {
    font-family: 'Cormorant Garamond', serif;
    font-size: 1rem;
    color: var(--white);
    letter-spacing: 0.05em;
  }

  .founder-title {
    font-size: 0.65rem;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: rgba(255,255,255,0.3);
  }

  .founder-content {
    padding-top: 1rem;
  }

  .founder-label {
    font-size: 0.65rem;
    letter-spacing: 0.25em;
    text-transform: uppercase;
    color: var(--gold);
    margin-bottom: 2rem;
    display: flex;
    align-items: center;
    gap: 1rem;
  }

  .founder-label::after {
    content: '';
    width: 40px;
    height: 0.5px;
    background: rgba(200,169,110,0.3);
  }

  .founder-story p {
    font-size: 1rem;
    line-height: 1.9;
    color: rgba(255,255,255,0.65);
    margin-bottom: 1.5rem;
    font-weight: 300;
  }

  .founder-story p:first-child {
    font-family: 'Cormorant Garamond', serif;
    font-size: 1.35rem;
    font-weight: 400;
    color: var(--white);
    line-height: 1.6;
  }

  /* THE NAME */
  .name-section {
    padding: 8rem 4rem;
    background: var(--black);
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 6rem;
    align-items: center;
  }

  .name-left {
    font-family: 'Cormorant Garamond', serif;
    font-weight: 300;
    font-size: clamp(4rem, 8vw, 8rem);
    line-height: 0.9;
    color: rgba(255,255,255,0.06);
    letter-spacing: -0.02em;
    user-select: none;
  }

  .name-left span {
    display: block;
    color: rgba(200,169,110,0.2);
  }

  .name-right {}

  .name-right .section-label {
    font-size: 0.65rem;
    letter-spacing: 0.25em;
    text-transform: uppercase;
    color: var(--gold);
    margin-bottom: 2rem;
    display: flex;
    align-items: center;
    gap: 1rem;
  }

  .name-right .section-label::after {
    content: '';
    width: 40px;
    height: 0.5px;
    background: rgba(200,169,110,0.3);
  }

  .name-right h3 {
    font-family: 'Cormorant Garamond', serif;
    font-weight: 400;
    font-size: 1.5rem;
    color: var(--white);
    margin-bottom: 1.5rem;
    line-height: 1.3;
  }

  .name-right p {
    font-size: 0.9rem;
    line-height: 1.9;
    color: rgba(255,255,255,0.5);
    margin-bottom: 1.25rem;
    font-weight: 300;
  }

  .name-right .standout {
    font-family: 'Cormorant Garamond', serif;
    font-size: 1.2rem;
    font-style: italic;
    color: var(--film);
    border-left: 1px solid var(--gold);
    padding-left: 1.5rem;
    margin-top: 2rem;
    opacity: 0.8;
  }

  /* CREDENTIALS */
  .credentials {
    padding: 6rem 4rem;
    background: var(--studio);
  }

  .cred-label {
    font-size: 0.65rem;
    letter-spacing: 0.25em;
    text-transform: uppercase;
    color: var(--gold);
    margin-bottom: 4rem;
    display: flex;
    align-items: center;
    gap: 1rem;
  }

  .cred-label::after {
    content: '';
    flex: 1;
    max-width: 60px;
    height: 0.5px;
    background: rgba(200,169,110,0.3);
  }

  .cred-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1px;
    background: rgba(255,255,255,0.05);
  }

  .cred-card {
    background: var(--studio);
    padding: 2.5rem;
    transition: background 0.3s;
  }

  .cred-card:hover { background: var(--shadow); }

  .cred-num {
    font-family: 'Cormorant Garamond', serif;
    font-size: 3.5rem;
    font-weight: 300;
    color: var(--white);
    line-height: 1;
    margin-bottom: 0.5rem;
  }

  .cred-num span {
    font-size: 1.5rem;
    color: var(--gold);
    vertical-align: super;
  }

  .cred-title {
    font-size: 0.75rem;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: rgba(255,255,255,0.4);
    margin-bottom: 0.75rem;
  }

  .cred-desc {
    font-size: 0.82rem;
    color: rgba(255,255,255,0.3);
    line-height: 1.7;
    font-weight: 300;
  }

  /* VALUES */
  .values {
    padding: 8rem 4rem;
    background: var(--black);
  }

  .values-label {
    font-size: 0.65rem;
    letter-spacing: 0.25em;
    text-transform: uppercase;
    color: var(--gold);
    margin-bottom: 4rem;
    display: flex;
    align-items: center;
    gap: 1rem;
  }

  .values-label::after {
    content: '';
    flex: 1;
    max-width: 60px;
    height: 0.5px;
    background: rgba(200,169,110,0.3);
  }

  .values-list {
    display: flex;
    flex-direction: column;
    gap: 0;
  }

  .value-row {
    display: grid;
    grid-template-columns: 200px 1fr;
    gap: 4rem;
    align-items: baseline;
    padding: 2.5rem 0;
    border-bottom: 0.5px solid rgba(255,255,255,0.05);
    transition: padding-left 0.4s;
  }

  .value-row:first-child { border-top: 0.5px solid rgba(255,255,255,0.05); }

  .value-row:hover { padding-left: 1rem; }

  .value-name {
    font-family: 'Cormorant Garamond', serif;
    font-size: 1.3rem;
    font-weight: 400;
    color: var(--white);
  }

  .value-desc {
    font-size: 0.88rem;
    color: rgba(255,255,255,0.45);
    line-height: 1.8;
    font-weight: 300;
  }

  /* TEAM NOTE */
  .team {
    padding: 6rem 4rem;
    background: var(--studio);
    border-top: 0.5px solid rgba(255,255,255,0.05);
  }

  .team-inner {
    max-width: 700px;
  }

  .team-label {
    font-size: 0.65rem;
    letter-spacing: 0.25em;
    text-transform: uppercase;
    color: var(--gold);
    margin-bottom: 2rem;
    display: flex;
    align-items: center;
    gap: 1rem;
  }

  .team-label::after {
    content: '';
    width: 40px;
    height: 0.5px;
    background: rgba(200,169,110,0.3);
  }

  .team-body p {
    font-size: 1rem;
    line-height: 1.9;
    color: rgba(255,255,255,0.55);
    margin-bottom: 1.25rem;
    font-weight: 300;
  }

  .team-body p:first-child {
    font-family: 'Cormorant Garamond', serif;
    font-size: 1.3rem;
    color: var(--white);
    font-weight: 400;
    line-height: 1.5;
  }

  /* CLOSER CTA */
  .closer {
    padding: 9rem 4rem;
    background: var(--black);
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    border-top: 0.5px solid rgba(255,255,255,0.04);
  }

  .closer-eyebrow {
    font-size: 0.65rem;
    letter-spacing: 0.25em;
    text-transform: uppercase;
    color: rgba(255,255,255,0.25);
    margin-bottom: 1.5rem;
  }

  .closer-headline {
    font-family: 'Cormorant Garamond', serif;
    font-weight: 300;
    font-size: clamp(2.5rem, 5vw, 4.5rem);
    color: var(--white);
    line-height: 1.1;
    margin-bottom: 1.5rem;
  }

  .closer-headline em {
    font-style: italic;
    color: var(--film);
  }

  .closer-sub {
    font-size: 0.85rem;
    color: rgba(255,255,255,0.3);
    margin-bottom: 3rem;
    letter-spacing: 0.05em;
  }

  .closer-btn {
    display: inline-block;
    font-size: 0.75rem;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: var(--black);
    background: var(--gold);
    padding: 1rem 2.5rem;
    text-decoration: none;
    transition: all 0.3s;
  }

  .closer-btn:hover { background: var(--film); }

  /* FOOTER */
  footer {
    padding: 3rem 4rem;
    background: var(--black);
    border-top: 0.5px solid rgba(255,255,255,0.06);
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .footer-logo {
    font-family: 'Outfit', sans-serif;
    font-weight: 200;
    font-size: 0.95rem;
    letter-spacing: 0.25em;
    text-transform: uppercase;
    color: rgba(255,255,255,0.3);
  }

  .footer-logo span { color: var(--gold); opacity: 0.6; }

  .footer-links {
    display: flex;
    gap: 2rem;
    list-style: none;
  }

  .footer-links a {
    font-size: 0.65rem;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: rgba(255,255,255,0.2);
    text-decoration: none;
    transition: color 0.3s;
  }

  .footer-links a:hover { color: rgba(255,255,255,0.5); }

  .footer-copy {
    font-size: 0.62rem;
    letter-spacing: 0.1em;
    color: rgba(255,255,255,0.15);
  }
</style>
</head>
<body>

<!-- NAV -->
<nav>
  <a href="index.html" class="nav-logo">ZENE<span>MATIC</span></a>
  <ul class="nav-links">
    <li><a href="work.html">Work</a></li>
    <li><a href="services.html">Services</a></li>
    <li><a href="about.html" class="active">About</a></li>
  </ul>
  <a href="contact.html" class="nav-cta">Start a project</a>
</nav>

<!-- PAGE HERO -->
<section class="page-hero">
  <p class="page-hero-label">The story behind the studio</p>
  <h1 class="page-hero-headline">Built on movement.<br><em>Driven by story.</em></h1>
</section>

<!-- FOUNDER SECTION -->
<section class="founder">
  <div class="founder-image-col">
    <div class="founder-image">
      <!-- Replace with: <img src="zen-portrait.jpg" alt="Zen Nguyen — Zenematic Studio" style="width:100%;height:100%;object-fit:cover;"> -->
      <div class="founder-image-placeholder">
        Replace with a cinematic portrait of Zen<br>On set · Camera in hand · In his element<br>Not a headshot. A directed portrait.
      </div>
    </div>
    <div class="founder-caption">
      <span class="founder-name">Zen Nguyen</span>
      <span class="founder-title">Founder & Creative Director</span>
    </div>
  </div>

  <div class="founder-content">
    <p class="founder-label">The founder</p>
    <div class="founder-story">
      <p>My name is Zen Nguyen.</p>
      <p>I was born in Vietnam, where movement was my first language. Before I ever picked up a camera, I was a dancer — learning how to tell a story with my body, how to command a room without saying a word. That instinct never left me. It just found a new medium.</p>
      <p>In 2013, I moved to the United States for college. A new country, a new language, a new lens on the world. Kansas City became home — a city that quietly holds more culture, more hunger, and more talent than most people realize.</p>
      <p>I started Zenematic because I believed something: that every brand has a cinematic story inside it. Most never find it. Not because it isn't there — but because nobody looked for it with the right eyes.</p>
      <p>I look for it. Every single project.</p>
    </div>
  </div>
</section>

<!-- THE NAME -->
<section class="name-section">
  <div class="name-left">
    ZEN<span>EMATIC</span>
  </div>
  <div class="name-right">
    <p class="section-label">Why Zenematic?</p>
    <h3>Zen is my name.<br>Cinematic is my obsession.</h3>
    <p>The two were always going to find each other.</p>
    <p>But it's more than a name. Zen — the philosophy — is about presence. Intention. Doing one thing with complete focus. That's how we approach every frame we shoot, every story we tell, every brand we work with.</p>
    <p class="standout">"Zenematic isn't just a name. It's a standard."</p>
  </div>
</section>

<!-- CREDENTIALS -->
<section class="credentials">
  <p class="cred-label">By the numbers</p>
  <div class="cred-grid">
    <div class="cred-card">
      <p class="cred-num">3<span>rd</span></p>
      <p class="cred-title">Year with Big 12 Media</p>
      <p class="cred-desc">Official tournament media coverage for one of the most prestigious college athletic conferences in the country. Three years running in Kansas City.</p>
    </div>
    <div class="cred-card">
      <p class="cred-num">10</p>
      <p class="cred-title">Years teaching dance in KC</p>
      <p class="cred-desc">Professional hip hop choreographer. Red Bull Dance Your Style — KC Champion 2024. Top 16 USA 2025. Movement is in the DNA of everything Zenematic creates.</p>
    </div>
    <div class="cred-card">
      <p class="cred-num">∞</p>
      <p class="cred-title">Stories left to tell</p>
      <p class="cred-desc">Sports, fashion, entertainment, culture. Local brands, global clients. Every project is a new story. We're just getting started.</p>
    </div>
  </div>
</section>

<!-- VALUES -->
<section class="values">
  <p class="values-label">How we work</p>
  <div class="values-list">
    <div class="value-row">
      <span class="value-name">Cinematic first</span>
      <p class="value-desc">Every project starts with a visual language. We don't shoot and figure it out in the edit. We know the story before the camera rolls. Every frame is intentional — never accidental.</p>
    </div>
    <div class="value-row">
      <span class="value-name">Intention over volume</span>
      <p class="value-desc">We don't take every project. We take the right ones and give them everything. Quality over quantity is not a philosophy — it's a practice. It shows in the work.</p>
    </div>
    <div class="value-row">
      <span class="value-name">Culture is the canvas</span>
      <p class="value-desc">Sports, fashion, entertainment, community. We work where culture lives and moves. Our lens is shaped by Vietnam, KC, dance, and a decade of reading rooms and reading crowds.</p>
    </div>
    <div class="value-row">
      <span class="value-name">Built to grow with you</span>
      <p class="value-desc">Zenematic isn't a one-time vendor. We build creative partnerships that compound over time. The best work happens when we understand your brand as well as you do.</p>
    </div>
  </div>
</section>

<!-- TEAM NOTE -->
<section class="team">
  <div class="team-inner">
    <p class="team-label">The studio</p>
    <div class="team-body">
      <p>Zenematic is led by Zen Nguyen and built on a growing network of Kansas City's most talented creatives.</p>
      <p>Editors, photographers, producers, and directors who share one standard: cinematic or nothing. We collaborate with the best in KC to bring every project to life at the level it deserves.</p>
      <p>We're building something here. Come be part of it.</p>
    </div>
  </div>
</section>

<!-- CLOSER CTA -->
<section class="closer">
  <p class="closer-eyebrow">You've heard the story</p>
  <h2 class="closer-headline">Now let's write<br><em>yours.</em></h2>
  <p class="closer-sub">Tell us about your project and let's create something worth remembering.</p>
  <a href="contact.html" class="closer-btn">Start a conversation</a>
</section>

<!-- FOOTER -->
<footer>
  <div class="footer-logo">ZENE<span>MATIC</span></div>
  <ul class="footer-links">
    <li><a href="work.html">Work</a></li>
    <li><a href="services.html">Services</a></li>
    <li><a href="about.html">About</a></li>
    <li><a href="contact.html">Contact</a></li>
    <li><a href="https://instagram.com/zenematic.studio" target="_blank">Instagram</a></li>
  </ul>
  <p class="footer-copy">© 2025 Zenematic Studio — Kansas City</p>
</footer>

</body>
</html>